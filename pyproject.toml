[project]
name = "ego-region"
version = "0.1.0"
description = "Interpretability pipelines over activation geometry"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.1",
    "numpy>=2.5.2",
    "scikit-learn>=1.9.0",
    "torch>=2.13.0",
    "transformer-lens>=3.8.0",
    "transformers>=5.14.1",
]
